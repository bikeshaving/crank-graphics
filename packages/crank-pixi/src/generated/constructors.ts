// Auto-generated Pixi.js constructor helpers
// Generated by scripts/generate-pixi-objects.ts

import * as PIXI from 'pixi.js';
import type { PixiTag } from './tag-mapping';

// Helper function to resolve textures (including URL references)
function resolveTexture(textureRef: any): PIXI.Texture {
  if (!textureRef) return PIXI.Texture.EMPTY;
  
  if (textureRef instanceof PIXI.Texture) {
    return textureRef;
  }
  
  if (typeof textureRef === "string") {
    // For generated constructors, we'll just use PIXI.Texture.from for now
    // URL reference resolution will be handled by property appliers
    return PIXI.Texture.from(textureRef);
  }
  
  return PIXI.Texture.EMPTY;
}

// Constructor options for classes whose constructor needs an options object.
// Children and event handlers are applied later, not through the constructor.
function constructorOptions(props: Record<string, any>): Record<string, any> {
  const options: Record<string, any> = {};
  for (const [key, value] of Object.entries(props)) {
    if (key === "children" || key.startsWith("on")) continue;
    options[key] = value;
  }

  return options;
}

// Flag to track if we've added prototype modifications
let prototypesModified = false;

export function createPixiObject(tag: PixiTag, PixiClass: any, props: Record<string, any>): any {
  // Add prototype modifications on first use
  if (!prototypesModified) {
    // Add setter for AnimatedSprite playing property to override readonly nature
    if (PIXI.AnimatedSprite && PIXI.AnimatedSprite.prototype) {
      Object.defineProperty(PIXI.AnimatedSprite.prototype, 'playing', {
        get() {
          return this.currentFrame < this.totalFrames && !this._paused;
        },
        set(value: boolean) {
          if (value) {
            this.play();
          } else {
            this.stop();
          }
        },
        configurable: true
      });
    }
    prototypesModified = true;
  }

  try {
    switch (tag) {
      case 'sprite':
        return new PixiClass();
      case 'rendercontainer': {
        try {
          return new PixiClass(constructorOptions(props));
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with options:`, constructorError);
          return new PixiClass({});
        }
      }
      case 'renderlayer':
        return new PixiClass();
      case 'container':
        return new PixiClass();
      case 'domcontainer':
        return new PixiClass();
      case 'graphics':
        return new PixiClass();
      case 'text':
        return new PixiClass();
      case 'meshplane': {
        try {
          return new PixiClass(constructorOptions(props));
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with options:`, constructorError);
          return new PixiClass({});
        }
      }
      case 'perspectivemesh': {
        try {
          return new PixiClass(constructorOptions(props));
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with options:`, constructorError);
          return new PixiClass({});
        }
      }
      case 'meshrope': {
        try {
          return new PixiClass(constructorOptions(props));
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with options:`, constructorError);
          return new PixiClass({});
        }
      }
      case 'meshsimple': {
        try {
          return new PixiClass(constructorOptions(props));
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with options:`, constructorError);
          return new PixiClass({});
        }
      }
      case 'particlecontainer':
        return new PixiClass();
      case 'animatedsprite': {
        try {
          // Auto-generated constructor using interface analysis
          const options = {
            textures: (props.textures || []).map(resolveTexture),
            ...(props.animationSpeed !== undefined && { animationSpeed: props.animationSpeed }),
            ...(props.autoPlay !== undefined && { autoPlay: props.autoPlay }),
            ...(props.autoUpdate !== undefined && { autoUpdate: props.autoUpdate }),
            ...(props.loop !== undefined && { loop: props.loop }),
            ...(props.onComplete !== undefined && { onComplete: props.onComplete }),
            ...(props.onFrameChange !== undefined && { onFrameChange: props.onFrameChange }),
            ...(props.onLoop !== undefined && { onLoop: props.onLoop }),
            ...(props.updateAnchor !== undefined && { updateAnchor: props.updateAnchor })
          };
          return new PixiClass(options);
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with auto-generated options:`, constructorError);
          return new PixiClass();
        }
      }
      case 'nineslicesprite': {
        try {
          // Auto-generated constructor using interface analysis
          const options = {
            texture: resolveTexture(props.texture) || PIXI.Texture.EMPTY,
            ...(props.leftWidth !== undefined && { leftWidth: props.leftWidth }),
            ...(props.topHeight !== undefined && { topHeight: props.topHeight }),
            ...(props.rightWidth !== undefined && { rightWidth: props.rightWidth }),
            ...(props.bottomHeight !== undefined && { bottomHeight: props.bottomHeight }),
            ...(props.width !== undefined && { width: props.width }),
            ...(props.height !== undefined && { height: props.height }),
            ...(props.roundPixels !== undefined && { roundPixels: props.roundPixels }),
            ...(props.anchor !== undefined && { anchor: props.anchor })
          };
          return new PixiClass(options);
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with auto-generated options:`, constructorError);
          return new PixiClass();
        }
      }
      case 'tilingsprite': {
        try {
          // Auto-generated constructor using interface analysis
          const options = {
            ...(props.anchor !== undefined && { anchor: props.anchor }),
            ...(props.tilePosition !== undefined && { tilePosition: props.tilePosition }),
            ...(props.tileScale !== undefined && { tileScale: props.tileScale }),
            ...(props.tileRotation !== undefined && { tileRotation: props.tileRotation }),
            ...(props.texture && { texture: resolveTexture(props.texture) }),
            ...(props.width !== undefined && { width: props.width }),
            ...(props.height !== undefined && { height: props.height }),
            ...(props.applyAnchorToTexture && { applyAnchorToTexture: resolveTexture(props.applyAnchorToTexture) }),
            ...(props.roundPixels !== undefined && { roundPixels: props.roundPixels })
          };
          return new PixiClass(options);
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with auto-generated options:`, constructorError);
          return new PixiClass();
        }
      }
      case 'bitmaptext': {
        try {
          // Auto-generated constructor using interface analysis
          const options = {
            ...(props.anchor !== undefined && { anchor: props.anchor }),
            ...(props.text !== undefined && { text: props.text }),
            ...(props.resolution !== undefined && { resolution: props.resolution }),
            ...(props.style !== undefined && { style: props.style }),
            ...(props.roundPixels !== undefined && { roundPixels: props.roundPixels })
          };
          return new PixiClass(options);
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with auto-generated options:`, constructorError);
          return new PixiClass();
        }
      }
      case 'splittext': {
        try {
          return new PixiClass(constructorOptions(props));
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with options:`, constructorError);
          return new PixiClass({});
        }
      }
      case 'htmltext': {
        try {
          // Auto-generated constructor using interface analysis
          const options = {
            ...(props.textureStyle !== undefined && { textureStyle: props.textureStyle }),
            ...(props.autoGenerateMipmaps !== undefined && { autoGenerateMipmaps: props.autoGenerateMipmaps })
          };
          return new PixiClass(options);
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with auto-generated options:`, constructorError);
          return new PixiClass();
        }
      }
      case 'splitbitmaptext': {
        try {
          return new PixiClass(constructorOptions(props));
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with options:`, constructorError);
          return new PixiClass({});
        }
      }
      case 'mesh': {
        try {
          // Auto-generated constructor using interface analysis
          const options = {
            geometry: props.geometry || new PIXI.MeshGeometry({
              positions: new Float32Array([0, 0, 100, 0, 100, 100, 0, 100]),
              uvs: new Float32Array([0, 0, 1, 0, 1, 1, 0, 1]),
              indices: new Uint32Array([0, 1, 2, 0, 2, 3])
            }),
            ...(props.shader && { shader: props.shader }),
            ...(props.state && { state: props.state }),
            ...(props.texture && { texture: resolveTexture(props.texture) }),
            ...(props.roundPixels !== undefined && { roundPixels: props.roundPixels })
          };
          return new PixiClass(options);
        } catch (constructorError) {
          console.warn(`Constructor for ${tag} failed with auto-generated options:`, constructorError);
          return new PixiClass();
        }
      }
      default:
        return new PixiClass();
    }
  } catch (error) {
    console.warn(`Failed to create ${tag} with constructor args, falling back to default:`, error);
    return new PixiClass();
  }
}
